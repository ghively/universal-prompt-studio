id: bge-m3-local
label: BGE-M3 (local)
provider: local
api_model_id: bge-m3
base_url: http://localhost:11434/v1
category: embedding
kind: embedding
card_depth: full
vram_class: 4gb
context_window: 8192
max_output_tokens: 0
pricing: { input_per_mtok_usd: 0, output_per_mtok_usd: 0 }
params: { effort: false, verbosity: false, temperature: false, logprobs: false }
sampling: { default_temperature: null, notes: "EMBEDDING model — /v1/embeddings. 1.2GB, MIT, 100+ languages. The hybrid specialist: dense + sparse (lexical) + multi-vector retrieval from one model." }
reasoning: { kind: none, cot_prompting: neutral, notes: "n/a — embedding model." }
formatting_idiom: "Plain text, no instruction prefixes. Use dense scores for semantic recall, sparse for keyword-exact queries; hybrid fusion beats either alone on mixed workloads."
structured_output: "n/a — returns vectors."
strengths: [dense+sparse hybrid in one model, 100+ languages, MIT license, the multilingual alternate to qwen3-embedding]
failure_modes:
  - hybrid retrieval needs a store that supports both signals (or manual fusion) — plain cosine-only pipelines waste its advantage
prompting_notes: |
  - The pick when queries mix keyword-exact and semantic intent, or the corpus spans many languages; pair with bge-reranker-v2-m3 (see LOCAL.md).
last_reviewed: 2026-07-17
sources: ["https://ollama.com/library/bge-m3"]
